// hooks/useTweaks.ts — convenience re-export of the Tweaks context hook.
export { useTweaks, TweaksProvider, TWEAKS_DEFAULTS } from '../context/TweaksContext';
export type {
  TweaksState,
  TextSize,
  FontChoice,
  LineSpacing,
  LetterSpacing,
  ColorMode,
  DashboardVariant,
  ChatStyle,
  OceanInsight,
} from '../context/TweaksContext';
